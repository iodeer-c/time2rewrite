# Copy to .env and fill in the real API keys referenced by config/llm.yaml.
# Parser and rewriter can point to the same provider key.
PARSER_API_KEY=your-llm-api-key
REWRITER_API_KEY=your-llm-api-key
PARSER_PROXY_URL=
REWRITER_PROXY_URL=
